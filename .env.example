# 定时任务用的 Anthropic API Key（以 sk-ant- 开头）
# 日常对话走 Subscription 不受影响；后台定时任务需要此 Key
# 复制本文件为 .env 并填入真实密钥：cp .env.example .env
ANTHROPIC_API_KEY=sk-ant-api03-你的密钥

# 扶摇 MCP（本地 Cursor · `.cursor/mcp.json` 用 ${env:FUYAO_API_KEY}）
# 注意：HTTP 型 MCP 不会自动读本文件，还需把变量写入 shell 环境，见下方说明
# FUYAO_API_KEY=sk-fuyao-你的密钥
# 写入 ~/.zshrc 后重开终端 / 或：export FUYAO_API_KEY=... 后用 `cursor .` 启动
# Automations 云端另在 dashboard 填 Header，不读本仓库 .env

# 推送通知（当前 A'：仅飞书 Webhook）
# 正式用法：Webhook URL 只贴在 Cursor Automations Instructions 文末「密钥」段，不要 commit
# 本机自测可写进 .env（已 gitignore）；见 scheduler/SETUP-A-prime.md
# FEISHU_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/你的token

# 可选：飞书截断脚注的仓库网页根（一般不用填，脚本会读 git remote）
# REPO_WEB_BASE=https://github.com/zhenfeng95/invest-agent
# 或：GITHUB_REPO=zhenfeng95/invest-agent
# 可选：强制指定飞书链接分支（默认 main；一般不用）
# REPO_BRANCH=main
